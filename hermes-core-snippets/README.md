# Patched Hermes core copies (for review)

These are the full post-patch versions of the three files touched by
`../patches/hermes-post-tool-round-control.patch`. Prefer applying the
unified diff to a real Hermes checkout; use these only to review the delta.
